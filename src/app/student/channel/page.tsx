'use client';

import React from 'react';
import { OpenChannelView } from '@/components/channel/open-channel-view';

export default function StudentChannelPage() {
  return <OpenChannelView backPath="/student" />;
}
